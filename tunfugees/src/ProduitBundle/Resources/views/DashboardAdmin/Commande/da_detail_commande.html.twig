{% extends './Default/dashboard.html.twig' %}

{% block body %}
    <title><h1> Détails de la commande </h1> </title>
    <div class="tab-content" >
        <div class="tab-pane active" id="users" aria-expanded="true">

            <table id="usersTab" class="table table-striped table-bordered" cellspacing="0" width="100%">
                <thead>
                <tr>
                    <th> Image </th>
                    <th> Titre </th>
                    <th> Description </th>
                    <th> Prix Produit  </th>
                </tr>
                <tbody>
                {% for a in annonces %}
                    {% for value in liste_ligne %}
                        {% if value.idCommande == tab.id_commande %}
                            {% if value.idprod == a.idprod %}
                                <tr>
                                    <td> {% if a.img != '' %}
                                            <img alt="User Pic" style="width:50px;height:50px"
                                                 src="{{ asset('uploads/Produit/photo/' ~ a.img) }}"
                                                 class="img-circle img-responsive">
                                        {% else %}
                                            <img alt="annonce_Pic" style="width:50px;" src="{{ asset('assets/img/NA.png') }}" width="50" height="50"
                                                 id="profile-image1" class="img-circle img-responsive">
                                        {% endif %}</td>
                                    <td>{{ a.nomProd}}</td>
                                    <td>{{ a.description }}</td>
                                    <td>{{ a.prix }}</td>
                                </tr>
                            {% endif %}
                        {% endif %}
                    {% endfor %}
                {% endfor %}
                </tbody>
            </table>

        </div>
    </div>
{% endblock %}